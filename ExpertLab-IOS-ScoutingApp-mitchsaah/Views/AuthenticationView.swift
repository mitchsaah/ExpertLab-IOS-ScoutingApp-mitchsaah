import SwiftUI

struct AuthenticationView: View {
    var body: some View {
        VStack {
            Text("Sign In")
                .font(.largeTitle)
                .padding()
        }
    }
}

#Preview {
    AuthenticationView()
}
