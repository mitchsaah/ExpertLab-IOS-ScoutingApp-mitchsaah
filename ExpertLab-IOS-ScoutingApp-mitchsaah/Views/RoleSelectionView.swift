import SwiftUI

struct RoleSelectionView: View {
    var body: some View {
        VStack(spacing: 20) {
            Text("Select Your Role")
                .font(.largeTitle)
        }
        .padding()
    }
}

#Preview {
    RoleSelectionView()
}
